# CLAUDE.md

Hermes source repo compatibility shim.

Read `AGENTS.md`, then `RESOLVER.md`, then use `./ops/verify`.
On Spark, the installed runtime venv is `/home/jackyujieyang/.local/share/hermes-agent-venv`.
